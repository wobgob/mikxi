server {
    listen 80 default_server;
    listen [::]:80 default_server;

    root /var/www/wobgob.com;

    index index.html;

    server_name wobgob.com www.wobgob.com;

    location / {
        try_files $uri $uri/ =404;
    }
}